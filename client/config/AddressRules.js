const ADDRESS_RULES = [
  "Avoid using short forms like Nr., Opp., Bldg.",
  "Mention nearby well-known landmark if available",
  "Select address type (Home / Work / Other)"
];

export default ADDRESS_RULES;